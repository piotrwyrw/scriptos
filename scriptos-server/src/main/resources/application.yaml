spring:
  application:
    name: scriptos-server
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/scriptos

springdoc:
  swagger-ui:
    path: /api-docs

server:
  port: 8080
scriptos:
  auth:
    unused-token-expiration: 2800
    hard-token-expiration: 5600
  groups:
    common-group-name: common
  storage:
    directory: scriptos-data
  admin:
    username: admin
    password: piotr2005